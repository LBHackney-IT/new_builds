<%= form.text_field :urn %>
<%= form.select :location_id, @scheme.locations.collect {|x| [x.name, x.id]} %>
<%= form.select :scheme_priority_id, @scheme.scheme_priorities.collect {|x| [x.name, x.id]} %>
<%= form.select :resident_id, @scheme.residents.collect {|x| [x.name, x.id]} %>
<%= form.text_area :description %>
<%= form.select :trade, Issue.trades, include_blank: true %>
<%= form.select :status, Issue::Statuses %>
